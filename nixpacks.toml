[phases.setup]
nixPkgs = ["python39", "git"]

[phases.install]
cmds = [
    "pip install dash>=2.14.0 plotly>=5.18.0 pandas",
    "pip install git+https://${GITHUB_TOKEN}@github.com/ai-emerald/emerald-component-library.git"
]

[start]
cmd = "python app.py"
